message("Adding internal plugin: Klatt")
set(pluginName "Klatt")
set(pluginSrc "${srcPluginsDir}/Klatt")
set(pluginCross "${crossDir}/plugins/Klatt")
set(pluginPlatform "${targetPlatform}/plugins/Klatt")
set(LINKLIBS )
add_definitions(-DSQUEAK_BUILTIN_PLUGIN)
list(APPEND sources  "${pluginSrc}/Klatt.c")
include_directories(${pluginSrc} ${pluginCross} ${targetPlatform}/plugins/${pluginName})
add_library(Klatt STATIC ${sources})
set(linkFlags "${linkFlags} -m32")
set_property(TARGET Klatt PROPERTY LINK_FLAGS "${linkFlags}")
IF (Klatt_dependencies)
add_dependencies(Klatt ${Klatt_dependencies})
ENDIF (Klatt_dependencies)
