message("Adding internal plugin: SurfacePlugin")
set(pluginName "SurfacePlugin")
set(pluginSrc "${srcPluginsDir}/SurfacePlugin")
set(pluginCross "${crossDir}/plugins/SurfacePlugin")
set(pluginPlatform "${targetPlatform}/plugins/SurfacePlugin")
set(LINKLIBS )
add_definitions(-DSQUEAK_BUILTIN_PLUGIN)
list(APPEND sources  "${pluginSrc}/SurfacePlugin.c")
include_directories(${pluginSrc} ${pluginCross} ${targetPlatform}/plugins/${pluginName})
set(sources "")
list(APPEND sources  "${pluginCross}/SurfacePlugin.c")
add_library(SurfacePlugin STATIC ${sources})
set(linkFlags "${linkFlags} -m32")
set_property(TARGET SurfacePlugin PROPERTY LINK_FLAGS "${linkFlags}")
IF (SurfacePlugin_dependencies)
add_dependencies(SurfacePlugin ${SurfacePlugin_dependencies})
ENDIF (SurfacePlugin_dependencies)
