message("Adding internal plugin: Matrix2x3Plugin")
set(pluginName "Matrix2x3Plugin")
set(pluginSrc "${srcPluginsDir}/Matrix2x3Plugin")
set(pluginCross "${crossDir}/plugins/Matrix2x3Plugin")
set(pluginPlatform "${targetPlatform}/plugins/Matrix2x3Plugin")
set(LINKLIBS )
add_definitions(-DSQUEAK_BUILTIN_PLUGIN)
list(APPEND sources  "${pluginSrc}/Matrix2x3Plugin.c")
include_directories(${pluginSrc} ${pluginCross} ${targetPlatform}/plugins/${pluginName})
add_library(Matrix2x3Plugin STATIC ${sources})
set(linkFlags "${linkFlags} -m32")
set_property(TARGET Matrix2x3Plugin PROPERTY LINK_FLAGS "${linkFlags}")
IF (Matrix2x3Plugin_dependencies)
add_dependencies(Matrix2x3Plugin ${Matrix2x3Plugin_dependencies})
ENDIF (Matrix2x3Plugin_dependencies)
