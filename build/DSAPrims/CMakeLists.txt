message("Adding internal plugin: DSAPrims")
set(pluginName "DSAPrims")
set(pluginSrc "${srcPluginsDir}/DSAPrims")
set(pluginCross "${crossDir}/plugins/DSAPrims")
set(pluginPlatform "${targetPlatform}/plugins/DSAPrims")
set(LINKLIBS )
add_definitions(-DSQUEAK_BUILTIN_PLUGIN)
list(APPEND sources  "${pluginSrc}/DSAPrims.c")
include_directories(${pluginSrc} ${pluginCross} ${targetPlatform}/plugins/${pluginName})
add_library(DSAPrims STATIC ${sources})
set(linkFlags "${linkFlags} -m32")
set_property(TARGET DSAPrims PROPERTY LINK_FLAGS "${linkFlags}")
IF (DSAPrims_dependencies)
add_dependencies(DSAPrims ${DSAPrims_dependencies})
ENDIF (DSAPrims_dependencies)
