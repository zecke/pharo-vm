message("Adding internal plugin: SerialPlugin")
set(pluginName "SerialPlugin")
set(pluginSrc "${srcPluginsDir}/SerialPlugin")
set(pluginCross "${crossDir}/plugins/SerialPlugin")
set(pluginPlatform "${targetPlatform}/plugins/SerialPlugin")
set(LINKLIBS )
add_definitions(-DSQUEAK_BUILTIN_PLUGIN)
list(APPEND sources  "${pluginSrc}/SerialPlugin.c")
include_directories(${pluginSrc} ${pluginCross} ${targetPlatform}/plugins/${pluginName})
list(APPEND sources  "${pluginPlatform}/sqUnixSerial")
add_library(SerialPlugin STATIC ${sources})
set(linkFlags "${linkFlags} -m32")
set_property(TARGET SerialPlugin PROPERTY LINK_FLAGS "${linkFlags}")
IF (SerialPlugin_dependencies)
add_dependencies(SerialPlugin ${SerialPlugin_dependencies})
ENDIF (SerialPlugin_dependencies)
