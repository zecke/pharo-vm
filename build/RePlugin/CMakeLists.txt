message("Adding external plugin: RePlugin")
set(pluginName "RePlugin")
set(pluginSrc "${srcPluginsDir}/RePlugin")
set(pluginCross "${crossDir}/plugins/RePlugin")
set(pluginPlatform "${targetPlatform}/plugins/RePlugin")
set(LINKLIBS )
list(APPEND sources  "${pluginSrc}/RePlugin.c")
include_directories(${pluginSrc} ${pluginCross} ${targetPlatform}/plugins/${pluginName})
list(APPEND sources  "${pluginCross}/pcre.c" "${pluginCross}/study.c" "${pluginCross}/get.c" "${pluginCross}/chartables.c")
add_library(RePlugin SHARED ${sources})
set(LIBRARY_OUTPUT_PATH ${outputDir})
set(linkFlags "${linkFlags} -m32")
target_link_libraries(RePlugin ${LINKLIBS})
set_property(TARGET RePlugin PROPERTY LINK_FLAGS "${linkFlags}")
IF (RePlugin_dependencies)
add_dependencies(RePlugin ${RePlugin_dependencies})
ENDIF (RePlugin_dependencies)
