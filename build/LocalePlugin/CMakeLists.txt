message("Adding internal plugin: LocalePlugin")
set(pluginName "LocalePlugin")
set(pluginSrc "${srcPluginsDir}/LocalePlugin")
set(pluginCross "${crossDir}/plugins/LocalePlugin")
set(pluginPlatform "${targetPlatform}/plugins/LocalePlugin")
set(LINKLIBS )
add_definitions(-DSQUEAK_BUILTIN_PLUGIN)
list(APPEND sources  "${pluginSrc}/LocalePlugin.c")
include_directories(${pluginSrc} ${pluginCross} ${targetPlatform}/plugins/${pluginName})
list(APPEND sources  "${pluginPlatform}/sqUnixLocale.c")
add_library(LocalePlugin STATIC ${sources})
set(linkFlags "${linkFlags} -m32")
set_property(TARGET LocalePlugin PROPERTY LINK_FLAGS "${linkFlags}")
IF (LocalePlugin_dependencies)
add_dependencies(LocalePlugin ${LocalePlugin_dependencies})
ENDIF (LocalePlugin_dependencies)
