message("Adding internal plugin: DropPlugin")
set(pluginName "DropPlugin")
set(pluginSrc "${srcPluginsDir}/DropPlugin")
set(pluginCross "${crossDir}/plugins/DropPlugin")
set(pluginPlatform "${targetPlatform}/plugins/DropPlugin")
set(LINKLIBS )
add_definitions(-DSQUEAK_BUILTIN_PLUGIN)
list(APPEND sources  "${pluginSrc}/DropPlugin.c")
include_directories(${pluginSrc} ${pluginCross} ${targetPlatform}/plugins/${pluginName})
include_directories(${crossDir}/plugins/FilePlugin)
list(APPEND sources  "${pluginPlatform}/sqUnixDragDrop")
add_library(DropPlugin STATIC ${sources})
set(linkFlags "${linkFlags} -m32")
set_property(TARGET DropPlugin PROPERTY LINK_FLAGS "${linkFlags}")
IF (DropPlugin_dependencies)
add_dependencies(DropPlugin ${DropPlugin_dependencies})
ENDIF (DropPlugin_dependencies)
