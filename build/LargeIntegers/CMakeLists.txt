message("Adding internal plugin: LargeIntegers")
set(pluginName "LargeIntegers")
set(pluginSrc "${srcPluginsDir}/LargeIntegers")
set(pluginCross "${crossDir}/plugins/LargeIntegers")
set(pluginPlatform "${targetPlatform}/plugins/LargeIntegers")
set(LINKLIBS )
add_definitions(-DSQUEAK_BUILTIN_PLUGIN)
list(APPEND sources  "${pluginSrc}/LargeIntegers.c")
include_directories(${pluginSrc} ${pluginCross} ${targetPlatform}/plugins/${pluginName})
add_library(LargeIntegers STATIC ${sources})
set(linkFlags "${linkFlags} -m32")
set_property(TARGET LargeIntegers PROPERTY LINK_FLAGS "${linkFlags}")
IF (LargeIntegers_dependencies)
add_dependencies(LargeIntegers ${LargeIntegers_dependencies})
ENDIF (LargeIntegers_dependencies)
