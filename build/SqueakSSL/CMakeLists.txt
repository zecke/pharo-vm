message("Adding external plugin: SqueakSSL")
set(pluginName "SqueakSSL")
set(pluginSrc "${srcPluginsDir}/SqueakSSL")
set(pluginCross "${crossDir}/plugins/SqueakSSL")
set(pluginPlatform "${targetPlatform}/plugins/SqueakSSL")
set(LINKLIBS )
list(APPEND sources  "${pluginSrc}/SqueakSSL.c")
include_directories(${pluginSrc} ${pluginCross} ${targetPlatform}/plugins/${pluginName})
list(APPEND sources  "${pluginPlatform}/sqUnixOpenSSL.c")
list(APPEND LINKLIBS ssl)
add_library(SqueakSSL SHARED ${sources})
set(LIBRARY_OUTPUT_PATH ${outputDir})
set(linkFlags "${linkFlags} -m32")
target_link_libraries(SqueakSSL ${LINKLIBS})
set_property(TARGET SqueakSSL PROPERTY LINK_FLAGS "${linkFlags}")
IF (SqueakSSL_dependencies)
add_dependencies(SqueakSSL ${SqueakSSL_dependencies})
ENDIF (SqueakSSL_dependencies)
