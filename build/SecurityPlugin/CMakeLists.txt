message("Adding internal plugin: SecurityPlugin")
set(pluginName "SecurityPlugin")
set(pluginSrc "${srcPluginsDir}/SecurityPlugin")
set(pluginCross "${crossDir}/plugins/SecurityPlugin")
set(pluginPlatform "${targetPlatform}/plugins/SecurityPlugin")
set(LINKLIBS )
add_definitions(-DSQUEAK_BUILTIN_PLUGIN)
list(APPEND sources  "${pluginSrc}/SecurityPlugin.c")
include_directories(${pluginSrc} ${pluginCross} ${targetPlatform}/plugins/${pluginName})
list(APPEND sources  "${pluginPlatform}/sqUnixSecurity")
add_library(SecurityPlugin STATIC ${sources})
set(linkFlags "${linkFlags} -m32")
set_property(TARGET SecurityPlugin PROPERTY LINK_FLAGS "${linkFlags}")
IF (SecurityPlugin_dependencies)
add_dependencies(SecurityPlugin ${SecurityPlugin_dependencies})
ENDIF (SecurityPlugin_dependencies)
